# Primary registry for pnpm workspace (frontend, social, shared packages).
# Backend (apps/backend) uses npm separately and does not read this file.
registry=https://mirror.abrha.net/repository/npm/

fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
